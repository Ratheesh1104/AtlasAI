# Frontend (Next.js) — copy the relevant line(s) into .env.local for dev,
# or set as real env vars in your hosting platform for production.
#
# Points the frontend at the Django backend. Configurable per environment —
# e.g. http://localhost:8000/api for local dev, https://api.yourapp.com/api in prod.
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Backend (Django) configuration now lives in backend/.env.example — see
# backend/.env.development and backend/.env.production.example for the full
# set of configurable options (database, CORS, LLM provider/keys, etc).
